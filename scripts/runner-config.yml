log:
  level: info

runner:
  file: /data/.runner
  capacity: 2
  timeout: 3h

container:
  network: bridge
  options: -v /var/run/docker.sock:/var/run/docker.sock
  valid_volumes:
    - /var/run/docker.sock

labels:
  - ubuntu-latest:docker://node:20-bookworm
  - deploy:host
